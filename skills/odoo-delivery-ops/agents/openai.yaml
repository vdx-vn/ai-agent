interface:
  display_name: "Odoo Delivery Ops"
  short_description: "CLI and runtime operations"
  default_prompt: "Use $odoo-delivery-ops to operational mechanics guidance covering commands, flags, environment behavior, and runtime cautions. If the answer is about test execution, point to $odoo-test and `odoo runtime-test` (odoo-cli)."

policy:
  allow_implicit_invocation: true
